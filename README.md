# 🤖 Телеграм Бот для Обучения

Телеграм бот для регистрации пользователей и отправки обучающих материалов с системой напоминаний.

## 📋 Оглавление

1. [Описание](#описание)
2. [Функционал](#функционал)
3. [Требования](#требования)
4. [Установка](#установка)
5. [Настройка](#настройка)
6. [Структура проекта](#структура-проекта)
7. [База данных](#база-данных)
8. [Запуск бота](#запуск-бота)
9. [Редактирование текстов](#редактирование-текстов)

---

## 📝 Описание

Бот предназначен для регистрации пользователей по email из базы данных Supabase и отправки им обучающих видеоматериалов. После отправки видео включается система напоминаний, которая мотивирует пользователя к просмотру.

## ✨ Функционал

### Реализованные функции:

#### 🔐 Этап 1: Регистрация
1. **Регистрация пользователей**
   - Команда `/start` для начала работы
   - Запрос и валидация email адреса
   - Проверка email в базе данных Supabase
   - Все email сохраняются и проверяются в нижнем регистре

2. **Отправка первого обучающего видео (learn1.mp4)**
   - Автоматическая отправка видео после успешной регистрации
   - Кнопка подтверждения просмотра видео

3. **Система напоминаний**
   - 1-е напоминание: через 10 минут после отправки видео
   - 2-е напоминание: через 1 час после отправки видео
   - 3-е напоминание: через 24 часа после отправки видео
   - Автоматическая отмена всех напоминаний при нажатии кнопки

#### 📢 Этап 2: Создание телеграм канала
4. **Вопрос о наличии канала**
   - После просмотра первого видео бот спрашивает о наличии телеграм канала
   - Две кнопки: "Канал создан, едем дальше" и "Нужно создать"

5. **Инструкция по созданию канала (если выбрано "Нужно создать")**
   - Отправка видео-инструкции (learn2.mp4)
   - Отправка текстовой инструкции (редактируется в файле messages.py)
   - Кнопка "Канал создан, едем дальше" для продолжения

#### 🎓 Этап 3: Следующий урок
6. **Отправка видео-урока (learn3.mp4)**
   - После подтверждения создания канала отправляется следующий урок
   - Две кнопки: "Нужна помощь" и "Едем дальше"

#### 🤝 Этап 4: Персонализированная помощь (AI)
7. **Обработка запроса "Нужна помощь"**
   - Запрос информации о пользователе (текст или голос)
   - Транскрибация голосовых сообщений через OpenAI Whisper
   - Формирование персонализированного промпта
   - Отправка запроса в n8n через webhook
   - Ожидание ответа (до 3 минут)
   - Показ персонализированных вариантов от AI

#### 📝 Этап 5: Наполнение канала
8. **Создание контента для канала (learn4.mp4)**
   - Видео-инструкция о наполнении канала
   - Две кнопки: "Напиши мне посты" и "Напишу сам"
   - Выбор способа создания контента

#### ✍️ Этап 6: Создание 5 постов с AI (опционально)
9. **Автоматическое создание 5 постов**
   - Для каждого из 5 постов:
     * Бот сообщает тему поста (из таблицы posts)
     * Задает 3 вопроса по очереди (текст или голос)
     * Подставляет ответы в промпт из БД
     * Отправляет в n8n для генерации
     * Ожидает ответ (до 3 минут)
     * Показывает готовый пост
   
10. **Система переписывания постов**
   - Две кнопки: "Переписать" и "Написать следующий пост"
   - Максимум 2 попытки на каждый пост
   - После второй попытки - автоматический переход к следующему
   - Голосовые ответы транскрибируются через OpenAI Whisper

#### 📢 Этап 7: Публикация поста-знакомства
11. **Видео-инструкция (learn5.mp4)**
   - Обучающее видео о публикации поста
   - Две кнопки: "Опубликую сам" и "Помоги опубликовать"

12. **Помощь в публикации (опционально)**
   - Запрос ссылки на канал
   - Проверка, что это канал (не группа)
   - Инструкция по добавлению бота админом
   - Проверка прав бота на публикацию
   
13. **Создание поста с кнопкой**
   - 5 вопросов о канале/продукте (текст или голос)
   - Запрос 5 ссылок на лучшие посты (с возможностью пропустить)
   - Генерация текста через n8n (промпт prompt_bluebutt)
   - Выбор действия кнопки: "В ЛС" или "На сайт"
   - Выбор текста кнопки (6 вариантов или свой)
   - Предпросмотр поста с кнопкой
   - Подтверждение и публикация на канале
   - Автоматическое закрепление поста

#### 📣 Этап 8: Создание анонсов для привлечения аудитории
13. **Видео-инструкция (learn6.mp4)**
   - Обучающее видео о создании анонсов
   - Две кнопки: "Напишу анонс сам" и "Напиши анонс за меня"

14. **Создание анонса с AI (опционально)**
   - Вопрос 1: О чем речь в посте (сохраняется как anons1)
   - Вопрос 2: Ссылка на пост (сохраняется как anons2)
   - Генерация анонса через n8n (промпт prompt_anons)
   - Ожидание ответа до 3 минут
   - Если нет ответа - повтор вопросов
   - Показ готового анонса
   - Автоматический переход к следующему этапу

#### 💰 Этап 9: Создание продающего поста
15. **Видео-инструкция (learn7.mp4)**
   - Обучающее видео о создании продающих постов
   - Две кнопки: "Напишу пост сам" и "Напиши продающий пост за меня"

16. **Создание продающего поста с AI (опционально)**
   - Вопрос 1: Что вы продаете? (сохраняется как prodaj1)
   - Вопрос 2: Какую проблему решает? (сохраняется как prodaj2)
   - Вопрос 3: Призыв к действию (сохраняется как prodaj3)
   - Генерация поста через n8n (промпт prompt_prodaj)
   - Ожидание ответа до 3 минут
   - Если нет ответа - повтор с 1 вопроса
   - Если есть ответ - показ поста с кнопками "Переписать" и "К финальному шагу"

17. **Система переписывания продающего поста**
   - При нажатии "Переписать" - повтор 3 вопросов
   - После переписывания - только кнопка "К финальному шагу"
   - Счетчик переписываний сохраняется в БД

#### 🎉 Этап 10: Финальный шаг
18. **Завершающие сообщения**
   - Поздравление с завершением курса
   - Список достижений пользователя
   - Мотивационное сообщение

#### 💾 Дополнительные возможности
14. **Сохранение состояний**
   - Все состояния пользователей сохраняются в базе данных
   - Возможность продолжить с того места, где остановился
   - Отслеживание прогресса каждого пользователя
   - Сохранение прогресса создания постов (номер поста, вопроса, попытки)
   - Сохранение данных канала и поста с кнопкой

---

## 🔧 Требования

- Python 3.8 или выше
- Аккаунт в Telegram (для создания бота через @BotFather)
- Аккаунт в Supabase (для базы данных)
- API ключ OpenAI (для транскрибации голосовых сообщений)
- n8n workflow (для обработки AI-запросов)

---

## 📦 Установка

### 1. Клонирование репозитория

```bash
cd PPTbot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
```

### 3. Активация виртуального окружения

**На macOS/Linux:**
```bash
source venv/bin/activate
```

**На Windows:**
```bash
venv\Scripts\activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

---

## ⚙️ Настройка

### 1. Создание бота в Telegram

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен

### 2. Настройка Supabase

1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Перейдите в раздел **"SQL Editor"**
4. **Откройте файл `setup.sql`** из проекта
5. **Скопируйте ВЕСЬ содержимое файла** и вставьте в SQL Editor
6. Нажмите "Run" - все таблицы будут созданы автоматически!

**Что создается:**
- ✅ Таблица **users** (пользователи с прогрессом постов)
- ✅ Таблица **prompts** (промпты для AI)
- ✅ Таблица **n8n_responses** (ответы от n8n)
- ✅ Таблица **posts** (5 постов с вопросами и промптами)
- ✅ Все индексы для быстрого поиска
- ✅ Примеры данных для тестирования

7. **ВАЖНО!** После создания таблиц отредактируйте:
   - Промпт `prompt_osebe` в таблице **prompts** (для помощи "О себе")
   - Промпт `prompt_bluebutt` в таблице **prompts** (для поста-знакомства)
   - Промпт `prompt_anons` в таблице **prompts** (для анонсов)
   - Промпт `prompt_prodaj` в таблице **prompts** (для продающего поста)
   - Все 5 постов в таблице **posts** (вопросы и промпты)

8. Добавьте тестовый email:
   ```sql
   INSERT INTO users (email) VALUES ('test@mail.com');
   ```

9. Получите API ключи:
   - Перейдите в Settings → API
   - Скопируйте `URL` и `anon/public` ключ

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Токен вашего Telegram бота от @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# URL вашего проекта Supabase
SUPABASE_URL=https://your-project.supabase.co

# Anon/Public ключ из Supabase
SUPABASE_KEY=your_supabase_anon_key_here

# Название таблиц (по умолчанию)
SUPABASE_TABLE=users
SUPABASE_PROMPTS_TABLE=prompts
SUPABASE_N8N_RESPONSES_TABLE=n8n_responses
SUPABASE_POSTS_TABLE=posts

# OpenAI API ключ
OPENAI_API_KEY=your_openai_api_key_here

# n8n Webhooks - отдельный webhook для каждого типа запроса
# Формат запроса для всех webhooks: {"telegram_id": int, "text": string, "request_id": string}

# Webhook для рассказа о себе (prompt_osebe)
N8N_WEBHOOK_OSEBE=https://your-n8n.domain/webhook/osebe

# Webhook для создания постов (prompt_post)
N8N_WEBHOOK_POST=https://your-n8n.domain/webhook/post

# Webhook для поста-знакомства (prompt_bluebutt)
N8N_WEBHOOK_BLUEBUTT=https://your-n8n.domain/webhook/bluebutt

# Webhook для анонсов (prompt_anons)
N8N_WEBHOOK_ANONS=https://your-n8n.domain/webhook/anons

# Webhook для продающего поста (prompt_prodaj)
N8N_WEBHOOK_PRODAJ=https://your-n8n.domain/webhook/prodaj
```

### 4. Получение API ключа OpenAI

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Создайте API ключ
3. Добавьте ключ в файл `.env`

### 5. Настройка n8n webhooks

**⚠️ ВАЖНО:** Для каждого типа AI-генерации используется отдельный webhook!

#### Структура запроса (одинаковая для всех webhooks):

```json
{
  "telegram_id": 123456789,
  "text": "Текст промпта или ответа пользователя",
  "request_id": "uuid-string"
}
```

#### 🔹 Webhook 1: OSEBE (Рассказ о себе)

**Назначение:** Генерация вариантов ниш для канала на основе рассказа пользователя о себе.

**Webhook URL:** `N8N_WEBHOOK_OSEBE`

**Когда используется:** Когда пользователь нажимает "Нужна помощь" после learn3.mp4 и отвечает на вопрос о себе.

**Path для ответа в бота:**
```
POST /bot/response/osebe
Content-Type: application/json

{
  "telegram_id": 123456789,
  "request_id": "uuid-string",
  "response": "Сгенерированный текст с вариантами ниш"
}
```

**Промпт берется из Supabase:** `prompts.prompt_osebe`

---

#### 🔹 Webhook 2: POST (Создание постов 1-5)

**Назначение:** Генерация постов для наполнения канала.

**Webhook URL:** `N8N_WEBHOOK_POST`

**Когда используется:** При создании каждого из 5 постов, когда пользователь отвечает на 3 вопроса.

**Path для ответа в бота:**
```
POST /bot/response/post
Content-Type: application/json

{
  "telegram_id": 123456789,
  "request_id": "uuid-string",
  "response": "Сгенерированный пост"
}
```

**Промпт берется из Supabase:** `posts.prompt_post` (для каждого из 5 постов)

**Особенности:**
- Для каждого поста свой промпт из таблицы `posts`
- Промпт уже содержит подставленные ответы пользователя на 3 вопроса
- Поддерживается переписывание (до 2 попыток на пост)

---

#### 🔹 Webhook 3: BLUEBUTT (Пост-знакомство)

**Назначение:** Генерация текста для поста-знакомства с кнопкой.

**Webhook URL:** `N8N_WEBHOOK_BLUEBUTT`

**Когда используется:** После ответа на 5 вопросов о канале/продукте и предоставления ссылок на лучшие посты.

**Path для ответа в бота:**
```
POST /bot/response/bluebutt
Content-Type: application/json

{
  "telegram_id": 123456789,
  "request_id": "uuid-string",
  "response": "Сгенерированный текст поста-знакомства"
}
```

**Промпт берется из Supabase:** `prompts.prompt_bluebutt`

**Особенности:**
- Промпт содержит ответы на 5 вопросов
- Может содержать ссылки на 5 лучших постов (опционально)

---

#### 🔹 Webhook 4: ANONS (Анонсы)

**Назначение:** Генерация анонса для привлечения аудитории.

**Webhook URL:** `N8N_WEBHOOK_ANONS`

**Когда используется:** После learn6.mp4, когда пользователь выбирает "Напиши анонс за меня" и отвечает на 2 вопроса.

**Path для ответа в бота:**
```
POST /bot/response/anons
Content-Type: application/json

{
  "telegram_id": 123456789,
  "request_id": "uuid-string",
  "response": "Сгенерированный анонс"
}
```

**Промпт берется из Supabase:** `prompts.prompt_anons`

**Особенности:**
- 2 вопроса: О чем речь в посте? + Ссылка на пост
- Данные сохраняются в `users` как `anons1` и `anons2`

---

#### 🔹 Webhook 5: PRODAJ (Продающий пост)

**Назначение:** Генерация продающего поста.

**Webhook URL:** `N8N_WEBHOOK_PRODAJ`

**Когда используется:** После learn7.mp4, когда пользователь выбирает "Напиши продающий пост за меня" и отвечает на 3 вопроса.

**Path для ответа в бота:**
```
POST /bot/response/prodaj
Content-Type: application/json

{
  "telegram_id": 123456789,
  "request_id": "uuid-string",
  "response": "Сгенерированный продающий пост"
}
```

**Промпт берется из Supabase:** `prompts.prompt_prodaj`

**Особенности:**
- 3 вопроса: Что продаете? + Какую проблему решает? + Призыв к действию
- Данные сохраняются в `users` как `prodaj1`, `prodaj2`, `prodaj3`
- Поддерживается переписывание (вопросы задаются заново)

---

#### 📝 Настройка n8n workflow

**Для каждого webhook создайте отдельный workflow в n8n:**

1. **Webhook Node** - принимает POST запрос с `telegram_id`, `text`, `request_id`
2. **AI Node** (ChatGPT/Claude/etc.) - обрабатывает `text` и генерирует ответ
3. **HTTP Request Node** - отправляет ответ обратно в бота на соответствующий path

**Пример настройки HTTP Request для ответа:**

```
Method: POST
URL: https://your-bot-domain/bot/response/{type}
Headers:
  Content-Type: application/json
Body:
{
  "telegram_id": {{$node["Webhook"].json["telegram_id"]}},
  "request_id": {{$node["Webhook"].json["request_id"]}},
  "response": {{$node["AI"].json["response"]}}
}
```

**Где `{type}`** - один из: `osebe`, `post`, `bluebutt`, `anons`, `prodaj`

---

#### ⚙️ Endpoint бота для приема ответов от n8n

Бот автоматически проверяет таблицу `n8n_responses` в Supabase каждые 5 секунд.

**n8n должен сохранять ответы в таблицу `n8n_responses`:**

```sql
INSERT INTO n8n_responses (telegram_id, request_id, response)
VALUES (123456789, 'uuid-string', 'Сгенерированный текст');
```

**Таймаут ожидания:** 3 минуты (180 секунд)

### 6. Добавление медиафайлов

Поместите следующие видеофайлы в папку `media/`:

1. **`learn1.mp4`** - Первое обучающее видео (отправляется после регистрации)
2. **`learn2.mp4`** - Видео-инструкция по созданию телеграм канала
3. **`learn3.mp4`** - Следующий видео-урок после создания канала
4. **`learn4.mp4`** - Видео о наполнении канала контентом
5. **`learn5.mp4`** - Видео о публикации поста-знакомства
6. **`learn6.mp4`** - Видео о создании анонсов для привлечения аудитории
7. **`learn7.mp4`** - Видео о создании продающих постов

Убедитесь, что все файлы имеют правильные названия и находятся в папке `media/`

---

## 📁 Структура проекта

```
PPTbot/
│
├── bot.py                 # Главный файл запуска бота
├── config.py              # Конфигурация и настройки
├── database.py            # Модуль для работы с Supabase
├── handlers.py            # Обработчики команд и сообщений
├── reminders.py           # Система напоминаний
├── messages.py            # Все тексты сообщений бота
├── logger.py              # Модуль логирования
├── openai_helper.py       # Модуль для работы с OpenAI (транскрибация)
├── n8n_helper.py          # Модуль для работы с n8n webhook
├── channel_helper.py      # Модуль для работы с каналами (проверка, публикация)
├── publish_handlers.py    # Обработчики публикации поста-знакомства
│
├── media/                 # Папка для медиафайлов
│   ├── learn1.mp4        # Первое обучающее видео
│   ├── learn2.mp4        # Видео по созданию канала
│   ├── learn3.mp4        # Третий видео-урок
│   ├── learn4.mp4        # Видео о наполнении канала
│   ├── learn5.mp4        # Видео о публикации поста
│   ├── learn6.mp4        # Видео о создании анонсов
│   └── learn7.mp4        # Видео о создании продающих постов
│
├── temp/                  # Папка для временных файлов (голосовые сообщения)
├── logs/                  # Папка для логов бота
│
├── requirements.txt       # Зависимости Python
├── setup.sql              # SQL запросы для настройки Supabase (ВАЖНО!)
├── .env                   # Переменные окружения (создать вручную)
├── .gitignore            # Файлы для игнорирования в git
└── README.md             # Документация
```

### Описание модулей:

- **bot.py** - Главный файл, запускает бота и регистрирует обработчики
- **config.py** - Все настройки бота, константы, состояния пользователей
- **database.py** - Класс Database для работы с Supabase (3 таблицы)
- **handlers.py** - Обработчики команд, сообщений, голоса и кнопок
- **reminders.py** - Система планирования и отправки напоминаний
- **messages.py** - Все тексты сообщений (для легкого редактирования)
- **openai_helper.py** - Транскрибация голосовых сообщений через OpenAI Whisper
- **n8n_helper.py** - Отправка запросов в n8n и получение ответов
- **channel_helper.py** - Проверка каналов и публикация постов
- **publish_handlers.py** - Обработчики для публикации поста с кнопкой

---

## 🗄️ База данных

### Структура таблицы users

Все данные пользователей хранятся в одной таблице `users` в Supabase:

| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | Уникальный идентификатор |
| email | text | Email пользователя |
| telegram_id | bigint | ID в Telegram |
| state | text | Текущее состояние пользователя |
| video_sent_at | timestamp | Время отправки видео |
| otvet_osebe | text | Ответ "О себе" |
| current_post_number | int | Номер текущего поста (1-5) |
| otvet1-otvet3 | text | Ответы на 3 вопроса для поста |
| post1_text-post5_text | text | Готовые тексты 5 постов |
| blueotvet1-blueotvet5 | text | Ответы на 5 вопросов для поста-знакомства |
| link1-link5 | text | Ссылки на лучшие посты |
| channel_link | text | Ссылка на канал пользователя |
| button_action | text | Действие кнопки (dm/website) |
| button_url | text | URL для кнопки |
| button_text | text | Текст кнопки |
| blue_post_text | text | Готовый пост-знакомство |
| anons1 | text | Ответ "О чем пост" для анонса |
| anons2 | text | Ссылка на пост для анонса |
| anons_text | text | Готовый текст анонса |
| prodaj1 | text | Ответ "Что продаете" |
| prodaj2 | text | Ответ "Какую проблему решает" |
| prodaj3 | text | Ответ "Призыв к действию" |
| sales_text | text | Готовый текст продающего поста |
| rewrite_count | int | Количество переписываний продающего поста |
| created_at | timestamp | Дата создания записи |
| updated_at | timestamp | Дата последнего обновления |

### Состояния пользователя (state)

- `new` - Новый пользователь
- `waiting_email` - Ожидает ввода email
- `registered` - Email подтвержден
- `video_sent` - Видео learn1.mp4 отправлено, ожидает просмотра
- `video_watched` - Первое видео просмотрено
- `channel_question` - Вопрос о наличии канала
- `channel_creating` - Создает канал (получил инструкцию)
- `channel_created` - Канал создан
- `learn3_sent` - Видео learn3.mp4 отправлено
- `waiting_help` - Запросил помощь
- `continue_learning` - Продолжает обучение
- `learn4_sent` - Видео learn4.mp4 отправлено
- `choosing_posts_option` - Выбирает вариант с постами
- `write_myself` - Выбрал "Напишу сам"
- `creating_posts` - Создает посты с AI
- `answering_post_questions` - Отвечает на вопросы для поста
- `processing_post` - Обработка поста через n8n
- `post_result_shown` - Результат поста показан
- `all_posts_completed` - Все 5 постов созданы
- `learn5_sent` - Видео learn5.mp4 отправлено
- `choosing_publish_option` - Выбирает вариант публикации поста
- `waiting_channel_link` - Ожидает ссылку на канал
- `waiting_admin_confirmation` - Ожидает подтверждения добавления админа
- `answering_intro_post_questions` - Отвечает на вопросы для поста-знакомства
- `waiting_best_post_link` - Ожидает ссылки на лучшие посты
- `processing_intro_post` - Обработка поста-знакомства через n8n
- `choosing_button_destination` - Выбирает куда ведет кнопка
- `waiting_website_link` - Ожидает ссылку на сайт
- `choosing_button_text` - Выбирает текст кнопки
- `waiting_custom_button_text` - Ожидает свой текст кнопки
- `preview_post` - Предпросмотр поста
- `post_published` - Пост опубликован
- `learn6_sent` - Видео learn6.mp4 отправлено
- `write_anons_myself` - Выбрал "Напишу анонс сам"
- `creating_anons` - Создает анонс с AI
- `answering_anons_questions` - Отвечает на вопросы для анонса
- `processing_anons` - Обработка анонса через n8n
- `anons_completed` - Анонс создан
- `learn7_sent` - Видео learn7.mp4 отправлено
- `write_sales_myself` - Выбрал "Напишу пост сам"
- `creating_sales_post` - Создает продающий пост с AI
- `answering_sales_questions` - Отвечает на вопросы для продающего поста
- `processing_sales_post` - Обработка продающего поста через n8n
- `sales_post_ready` - Продающий пост готов
- `rewriting_sales_post` - Переписывание продающего поста
- `final_step` - Финальный шаг

### Добавление email в базу данных

Для добавления новых пользователей в базу данных:

1. Откройте Supabase → Table Editor → users
2. Нажмите "Insert row"
3. Заполните поле `email` (обязательно в нижнем регистре)
4. Остальные поля заполнятся автоматически

**Или через SQL:**

```sql
INSERT INTO users (email) VALUES ('example@mail.com');
INSERT INTO users (email) VALUES ('user2@gmail.com');
```

---

## 🚀 Запуск бота

### Обычный запуск

```bash
python bot.py
```

### Запуск в фоновом режиме (Linux/macOS)

```bash
nohup python bot.py &
```

### Остановка бота

Нажмите `Ctrl+C` в терминале, где запущен бот.

---

## 🐳 Запуск в Docker

### Преимущества Docker

- ✅ Изолированная среда
- ✅ Не нужно устанавливать Python и зависимости
- ✅ Автоматический перезапуск при сбое
- ✅ Простое развертывание на сервере
- ✅ Легкое обновление

### Быстрый старт

1. **Скопируйте `.env.example` в `.env`:**

```bash
cp .env.example .env
nano .env
```

2. **Заполните переменные окружения:**

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш_токен_бота

# Supabase (ваш развернутый сервер)
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=ваш_supabase_anon_key

# OpenAI
OPENAI_API_KEY=ваш_openai_api_key

# n8n Webhooks (ваш развернутый сервер)
N8N_WEBHOOK_OSEBE=https://your-n8n.domain/webhook/osebe
N8N_WEBHOOK_POST=https://your-n8n.domain/webhook/post
N8N_WEBHOOK_BLUEBUTT=https://your-n8n.domain/webhook/bluebutt
N8N_WEBHOOK_ANONS=https://your-n8n.domain/webhook/anons
N8N_WEBHOOK_PRODAJ=https://your-n8n.domain/webhook/prodaj
```

3. **Добавьте медиафайлы в папку `media/`:**

```bash
# Поместите видеофайлы learn1.mp4 - learn7.mp4
ls media/
```

4. **Запустите бота:**

```bash
# Собрать и запустить
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f bot
```

### Команды управления

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Перезапуск
docker-compose restart

# Остановка и удаление
docker-compose down

# Обновление (после изменения кода)
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot
docker-compose logs --tail=100 bot

# Вход в контейнер
docker exec -it pptbot-telegram-bot /bin/bash
```

### Альтернативный запуск через Docker

```bash
# Собрать образ
docker build -t pptbot .

# Запустить контейнер
docker run -d --name pptbot-bot \
  --env-file .env \
  -v $(pwd)/media:/app/media \
  -v $(pwd)/logs:/app/logs \
  -v $(pwd)/temp:/app/temp \
  --restart unless-stopped \
  pptbot

# Просмотр логов
docker logs -f pptbot-bot

# Остановка
docker stop pptbot-bot

# Удаление
docker rm pptbot-bot
```

### Подключение к Supabase и n8n в той же сети Docker

Если ваш Supabase и n8n находятся в Docker на том же сервере:

1. Узнайте имя сети:

```bash
docker network ls
```

2. Раскомментируйте в `docker-compose.yml`:

```yaml
networks:
  - external-network

networks:
  external-network:
    external: true
    name: your-network-name  # Замените на имя вашей сети
```

3. В `.env` используйте имена контейнеров:

```bash
SUPABASE_URL=http://supabase-kong:8000
N8N_WEBHOOK_OSEBE=http://n8n:5678/webhook/osebe
N8N_WEBHOOK_POST=http://n8n:5678/webhook/post
N8N_WEBHOOK_BLUEBUTT=http://n8n:5678/webhook/bluebutt
N8N_WEBHOOK_ANONS=http://n8n:5678/webhook/anons
N8N_WEBHOOK_PRODAJ=http://n8n:5678/webhook/prodaj
```

### Автозапуск при перезагрузке сервера

Docker Compose с `restart: unless-stopped` автоматически запустит бота после перезагрузки.

Проверка:

```bash
# Перезагрузить сервер
sudo reboot

# После перезагрузки
docker ps | grep pptbot
```

### Troubleshooting

**Бот не запускается:**

```bash
# Проверить логи
docker logs pptbot-telegram-bot

# Проверить переменные
docker exec pptbot-telegram-bot env | grep TELEGRAM
```

**Не видит медиафайлы:**

```bash
# Проверить монтирование
docker exec pptbot-telegram-bot ls -la /app/media
```

**Не подключается к Supabase/n8n:**

```bash
# Проверить доступность Supabase
docker exec pptbot-telegram-bot curl -I $SUPABASE_URL

# Проверить доступность n8n webhooks
docker exec pptbot-telegram-bot curl -I $N8N_WEBHOOK_OSEBE
docker exec pptbot-telegram-bot curl -I $N8N_WEBHOOK_POST
```

---

## 📊 Система логирования

Бот оснащен подробной системой логирования для мониторинга и отладки.

### Файлы логов

Логи автоматически сохраняются в папку `logs/`:
- Формат имени: `bot_ГГГГММДД.log`
- Новый файл создается каждый день
- Логи также выводятся в консоль

### Использование скрипта logs.sh

Для удобного просмотра логов используйте скрипт `logs.sh`:

```bash
# Сделать скрипт исполняемым (один раз)
chmod +x logs.sh

# Запустить интерактивное меню
./logs.sh

# Или использовать прямые команды:
./logs.sh user 123456789     # Логи конкретного пользователя
./logs.sh errors             # Только ошибки
./logs.sh warnings           # Только предупреждения
./logs.sh n8n                # Взаимодействие с n8n
./logs.sh posts              # Создание постов
./logs.sh publish            # Публикация постов
./logs.sh anons              # Анонсы
./logs.sh sales              # Продающие посты
./logs.sh voice              # Голосовые сообщения
./logs.sh reminders          # Напоминания
./logs.sh database           # Запросы к БД
./logs.sh video              # Отправка видео
./logs.sh today              # Всё за сегодня
./logs.sh live               # Живые логи (Ctrl+C для выхода)
./logs.sh stats              # Статистика из БД
./logs.sh search "текст"     # Поиск по тексту
```

### Интерактивное меню

При запуске `./logs.sh` без параметров открывается меню:

```
╔══════════════════════════════════════════════════════════╗
║          📊 СИСТЕМА ЛОГИРОВАНИЯ БОТА PPTbot             ║
╠══════════════════════════════════════════════════════════╣
║                                                          ║
║  1) 👤 Логи пользователя (по telegram_id)                ║
║  2) ❌ Только ERROR                                      ║
║  3) ⚠️  Только WARNING                                   ║
║  4) 🤖 Взаимодействие с n8n                              ║
║  5) 📝 Создание постов                                   ║
║  6) 📤 Публикация поста-знакомства                       ║
║  7) 📣 Анонсы                                            ║
║  8) 💰 Продающие посты                                   ║
║  9) 🎤 Голосовые сообщения                               ║
║  10) 🔔 Напоминания                                      ║
║  11) 💾 Запросы к базе данных                            ║
║  12) 🎥 Отправка видео                                   ║
║  13) 📅 Всё за сегодня                                   ║
║  14) 🔴 Живые логи (Ctrl+C для выхода)                   ║
║  15) 📈 Статистика из БД                                 ║
║  16) 🔍 Поиск по тексту                                  ║
╚══════════════════════════════════════════════════════════╝
```

### Категории логов

Логи разделены по категориям для удобной фильтрации:

- **USER** - Действия пользователя (старт, регистрация, сообщения)
- **VIDEO** - Отправка и просмотр видео
- **N8N** - Взаимодействие с n8n (запросы, ответы, таймауты)
- **POSTS** - Создание постов 1-5
- **PUBLISH** - Публикация поста-знакомства
- **ANONS** - Создание анонсов
- **SALES** - Создание продающих постов
- **VOICE** - Обработка голосовых сообщений
- **REMINDER** - Напоминания
- **DATABASE** - Запросы к базе данных
- **SYSTEM** - Системные события (запуск, остановка)

### Формат логов

```
2024-12-15 14:30:45 | INFO     | USER | 👤 Пользователь запустил бота | telegram_id=123456789 | username=@user | first_name=Ivan
2024-12-15 14:30:50 | INFO     | USER | ✅ Пользователь зарегистрирован | telegram_id=123456789 | email=user@mail.com
2024-12-15 14:31:00 | INFO     | VIDEO | 🎥 Отправлено видео: learn1.mp4 | telegram_id=123456789
2024-12-15 14:35:20 | INFO     | N8N | 📤 Запрос отправлен в n8n | telegram_id=123456789 | request_id=abc123 | prompt=prompt_osebe
2024-12-15 14:35:45 | INFO     | N8N | 📥 Ответ получен от n8n | telegram_id=123456789 | request_id=abc123 | length=500 символов
2024-12-15 14:40:00 | ERROR    | N8N | ❌ Ошибка n8n: Connection timeout | telegram_id=123456789
```

### Добавление логирования в код

Для добавления логов в свой код см. файл `LOGGING_EXAMPLES.md`

Пример:

```python
from logger import bot_logger

# В обработчиках
bot_logger.user_button_click(telegram_id, callback_data)
bot_logger.n8n_request_sent(telegram_id, request_id, "prompt_osebe")
bot_logger.error('N8N', 'Ошибка подключения', telegram_id=telegram_id)
```

### Мониторинг в реальном времени

Для наблюдения за логами в реальном времени:

```bash
./logs.sh live
```

Или напрямую:

```bash
tail -f logs/bot_$(date +%Y%m%d).log
```

### Поиск проблем

Быстрый способ найти все ошибки за сегодня:

```bash
./logs.sh errors
```

Поиск по конкретному пользователю:

```bash
./logs.sh user 123456789
```

Поиск по ключевым словам:

```bash
./logs.sh search "timeout"
./logs.sh search "published"
```

---

## ✏️ Редактирование текстов

Все тексты сообщений бота находятся в файле **`messages.py`**.

### Доступные тексты:

#### Регистрация и первое видео:
1. **START_MESSAGE** - Приветственное сообщение
2. **REQUEST_EMAIL** - Запрос email
3. **EMAIL_NOT_FOUND** - Email не найден в БД
4. **INVALID_EMAIL** - Некорректный email
5. **REGISTRATION_SUCCESS** - Успешная регистрация
6. **VIDEO_SENT_MESSAGE** - Сообщение после отправки видео
7. **BUTTON_VIDEO_WATCHED** - Текст кнопки "Видео просмотрено"
8. **VIDEO_WATCHED_MESSAGE** - Сообщение после нажатия кнопки

#### Напоминания:
9. **REMINDER_1** - Первое напоминание (10 минут)
10. **REMINDER_2** - Второе напоминание (1 час)
11. **REMINDER_3** - Третье напоминание (24 часа)

#### Вопрос о канале:
12. **CHANNEL_QUESTION_MESSAGE** - Вопрос о наличии канала
13. **BUTTON_CHANNEL_CREATED** - Текст кнопки "Канал создан"
14. **BUTTON_NEED_CREATE_CHANNEL** - Текст кнопки "Нужно создать"

#### Инструкция по созданию канала:
15. **CHANNEL_CREATION_INTRO** - Вводное сообщение
16. **CHANNEL_CREATION_TEXT_INSTRUCTION** - Текстовая инструкция (РЕДАКТИРУЙ ЭТОТ ТЕКСТ!)
17. **CHANNEL_CREATION_COMPLETE_MESSAGE** - Сообщение после инструкции

#### Видео learn3 и дальнейшие действия:
18. **CHANNEL_READY_MESSAGE** - Сообщение после создания канала
19. **LEARN3_VIDEO_MESSAGE** - Сообщение после отправки learn3.mp4
20. **BUTTON_NEED_HELP** - Текст кнопки "Нужна помощь"
21. **BUTTON_CONTINUE** - Текст кнопки "Едем дальше"

### Как редактировать:

1. Откройте файл `messages.py`
2. Найдите нужную переменную
3. Измените текст между тройными кавычками `"""`
4. Сохраните файл
5. Перезапустите бота

**Важно:** Все сообщения используют HTML форматирование:
- `<b>текст</b>` - жирный текст
- `<i>текст</i>` - курсив
- `<code>текст</code>` - моноширинный шрифт
- `<a href="url">текст</a>` - ссылка

---

## 🔧 Настройка времени напоминаний

Время отправки напоминаний можно изменить в файле **`config.py`**:

```python
# Время для напоминаний (в секундах)
REMINDER_1_DELAY = 10 * 60      # 10 минут
REMINDER_2_DELAY = 60 * 60      # 1 час
REMINDER_3_DELAY = 24 * 60 * 60 # 24 часа
```

---

## 📞 Поддержка

При возникновении проблем проверьте:

1. ✅ Правильность токена бота в `.env`
2. ✅ Правильность настроек Supabase в `.env`
3. ✅ Наличие видеофайла `learn1.mp4` в папке `media/`
4. ✅ Наличие email в базе данных Supabase
5. ✅ Активацию виртуального окружения
6. ✅ Установку всех зависимостей из `requirements.txt`

---

## 🚀 Развертывание на сервере

### Быстрое развертывание с GitHub

```bash
# 1. Клонировать репозиторий
git clone https://github.com/egorxnocode/pptbot.git
cd pptbot

# 2. Создать .env из примера
cp .env.example .env
nano .env

# 3. Заполнить переменные окружения:
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш_supabase_anon_key
OPENAI_API_KEY=ваш_openai_api_key
N8N_WEBHOOK_OSEBE=https://your-n8n.domain/webhook/osebe
N8N_WEBHOOK_POST=https://your-n8n.domain/webhook/post
N8N_WEBHOOK_BLUEBUTT=https://your-n8n.domain/webhook/bluebutt
N8N_WEBHOOK_ANONS=https://your-n8n.domain/webhook/anons
N8N_WEBHOOK_PRODAJ=https://your-n8n.domain/webhook/prodaj

# 4. Загрузить медиафайлы
# Поместите learn1.mp4 - learn7.mp4 в папку media/

# 5. Запустить в Docker
docker-compose up -d

# 6. Проверить статус
docker-compose ps
docker-compose logs -f bot
```

### Пошаговая инструкция

#### 1. Подготовка сервера

```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установить Docker Compose
sudo apt install docker-compose -y

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Проверить установку
docker --version
docker-compose --version
```

#### 2. Клонирование и настройка

```bash
# Клонировать репозиторий
cd /opt
sudo git clone https://github.com/egorxnocode/pptbot.git
cd pptbot
sudo chown -R $USER:$USER .

# Создать .env
cp .env.example .env
nano .env
```

#### 3. Настройка Supabase

1. Откройте ваш Supabase проект
2. Перейдите в Settings → API
3. Скопируйте:
   - `URL` → в `SUPABASE_URL`
   - `anon/public` ключ → в `SUPABASE_KEY`

4. Перейдите в SQL Editor
5. Вставьте содержимое файла `setup.sql`
6. Нажмите "Run"

7. Добавьте тестовые email в таблицу `users`:
```sql
INSERT INTO users (email) VALUES ('test@mail.com');
```

8. Отредактируйте промпты в таблице `prompts` под свои нужды

#### 4. Настройка n8n

Создайте **5 отдельных workflows** для каждого типа запроса:

**Workflow 1: OSEBE (Рассказ о себе)**
1. Webhook node → Path: `/webhook/osebe`
2. AI node → генерация вариантов ниш
3. HTTP Request node → сохранение в Supabase `n8n_responses`
4. Активируйте → скопируйте URL → `N8N_WEBHOOK_OSEBE`

**Workflow 2: POST (Создание постов)**
1. Webhook node → Path: `/webhook/post`
2. AI node → генерация поста
3. HTTP Request node → сохранение в Supabase
4. Активируйте → скопируйте URL → `N8N_WEBHOOK_POST`

**Workflow 3: BLUEBUTT (Пост-знакомство)**
1. Webhook node → Path: `/webhook/bluebutt`
2. AI node → генерация поста с кнопкой
3. HTTP Request node → сохранение в Supabase
4. Активируйте → скопируйте URL → `N8N_WEBHOOK_BLUEBUTT`

**Workflow 4: ANONS (Анонсы)**
1. Webhook node → Path: `/webhook/anons`
2. AI node → генерация анонса
3. HTTP Request node → сохранение в Supabase
4. Активируйте → скопируйте URL → `N8N_WEBHOOK_ANONS`

**Workflow 5: PRODAJ (Продающий пост)**
1. Webhook node → Path: `/webhook/prodaj`
2. AI node → генерация продающего поста
3. HTTP Request node → сохранение в Supabase
4. Активируйте → скопируйте URL → `N8N_WEBHOOK_PRODAJ`

**См. подробную документацию по каждому webhook выше в разделе "Настройка n8n webhooks"**

#### 5. Загрузка медиафайлов

```bash
# Создать папку media если её нет
mkdir -p media

# Загрузить видео (пример с scp)
scp learn*.mp4 user@your-server:/opt/pptbot/media/

# Или через wget/curl если у вас есть ссылки
# cd media
# wget https://ваш-сервер/learn1.mp4
# wget https://ваш-сервер/learn2.mp4
# ... и так далее

# Проверить
ls -lh media/
```

#### 6. Запуск бота

```bash
# Запустить в Docker
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f bot

# Если всё работает, логи покажут:
# "🚀 Бот успешно запущен"
```

#### 7. Проверка работы

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Введите email из базы данных
5. Проверьте получение видео

### Управление на сервере

```bash
# Просмотр логов
docker-compose logs -f bot
docker-compose logs --tail=100 bot

# Использование скрипта логирования
chmod +x logs.sh
./logs.sh live
./logs.sh errors
./logs.sh user 123456789

# Перезапуск бота
docker-compose restart

# Остановка бота
docker-compose stop

# Обновление бота (быстрый способ)
./update-bot.sh

# Обновление бота (вручную)
cd /opt/pptbot
git pull
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Проверка ресурсов
docker stats pptbot-telegram-bot
```

### Автозапуск при перезагрузке сервера

Docker Compose с `restart: unless-stopped` автоматически запускает бота после перезагрузки.

Проверка:
```bash
sudo reboot
# После перезагрузки
docker ps | grep pptbot
```

### Подключение к сети с Supabase и n8n

Если Supabase и n8n в той же Docker сети на сервере:

```bash
# Узнать имя сети
docker network ls

# Подключить бота (раскомментировать в docker-compose.yml)
networks:
  - your-network-name

networks:
  your-network-name:
    external: true
```

В `.env` использовать имена контейнеров:
```bash
SUPABASE_URL=http://supabase-kong:8000
N8N_WEBHOOK_OSEBE=http://n8n:5678/webhook/osebe
N8N_WEBHOOK_POST=http://n8n:5678/webhook/post
N8N_WEBHOOK_BLUEBUTT=http://n8n:5678/webhook/bluebutt
N8N_WEBHOOK_ANONS=http://n8n:5678/webhook/anons
N8N_WEBHOOK_PRODAJ=http://n8n:5678/webhook/prodaj
```

### Мониторинг

```bash
# Статус контейнера
docker ps | grep pptbot

# Использование ресурсов
docker stats pptbot-telegram-bot

# Проверка здоровья
docker inspect --format='{{.State.Health.Status}}' pptbot-telegram-bot

# Логи в реальном времени
./logs.sh live

# Только ошибки
./logs.sh errors
```

### Troubleshooting на сервере

**Бот не запускается:**
```bash
docker logs pptbot-telegram-bot
docker exec pptbot-telegram-bot env | grep TELEGRAM
```

**Нет подключения к Supabase:**
```bash
docker exec pptbot-telegram-bot curl -I $SUPABASE_URL
```

**Нет подключения к n8n:**
```bash
docker exec pptbot-telegram-bot curl -I $N8N_WEBHOOK_OSEBE
docker exec pptbot-telegram-bot curl -I $N8N_WEBHOOK_POST
```

**Не видит медиафайлы:**
```bash
docker exec pptbot-telegram-bot ls -la /app/media
ls -la media/
```

### Безопасность

1. **Firewall:** Откройте только необходимые порты
```bash
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 443/tcp   # HTTPS (если нужен)
sudo ufw enable
```

2. **Регулярные обновления:**
```bash
sudo apt update && sudo apt upgrade -y
docker-compose pull
```

3. **Бэкапы:**
```bash
# Бэкап .env
cp .env .env.backup

# Бэкап медиафайлов
tar -czf media_backup_$(date +%Y%m%d).tar.gz media/

# Бэкап логов
tar -czf logs_backup_$(date +%Y%m%d).tar.gz logs/
```

### Полезные команды

```bash
# Войти в контейнер
docker exec -it pptbot-telegram-bot /bin/bash

# Перезапуск без простоя
docker-compose up -d --no-deps --build bot

# Очистка старых образов
docker system prune -a

# Просмотр всех контейнеров
docker ps -a

# Удаление остановленных контейнеров
docker container prune
```

---

## 📝 Лицензия

Проект создан для внутреннего использования.

---

**Репозиторий:** https://github.com/egorxnocode/pptbot  
**Дата создания:** Декабрь 2025  
**Версия:** 1.0

